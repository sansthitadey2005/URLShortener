# URL Shortener API

A URL Shortener REST API built using Django and Django REST Framework.

## Features
- Generate short URLs
- Redirect to original URLs
- REST API support
- Random short code generation

## Tech Stack
- Python
- Django
- Django REST Framework
- SQLite

## Run locally

pip install -r requirements.txt
python manage.py migrate
python manage.py runserver
